import type { ReactNode } from 'react'; import { NavLink, useLocation } from 'react-router-dom'; import { FileText, LayoutDashboard, LogIn, ReceiptText, ShoppingCart } from 'lucide-react'; import { useAuth } from '@/context/AuthContext'; import { EmptyState } from '@/components/common/EmptyState'; import { formatCurrency } from '@/lib/format'; import { usePortalDashboard, usePortalInvoices, usePortalOrders, usePortalPayments } from '@/hooks/usePortal'; import type { PortalRole } from '@/types/portal';
const links = [{ path: '/portal', label: 'Overview', icon: LayoutDashboard }, { path: '/portal/invoices', label: 'Invoices / Bills', icon: FileText }, { path: '/portal/orders', label: 'Orders', icon: ShoppingCart }, { path: '/portal/payments', label: 'Payments', icon: ReceiptText }];
const Table = ({ headings, children }: { headings: string[]; children: ReactNode }) => <div className="overflow-x-auto rounded-lg border bg-white"><table className="min-w-[680px] w-full text-sm"><thead className="bg-[#f9fafb] text-left text-xs uppercase text-[#6b7280]"><tr>{headings.map(heading => <th key={heading} className="p-4">{heading}</th>)}</tr></thead><tbody>{children}</tbody></table></div>;
export function PartnerPortal() { const { user } = useAuth(); const location = useLocation(); const role = user?.role === 'CUSTOMER' || user?.role === 'VENDOR' ? user.role as PortalRole : undefined; const dashboard = usePortalDashboard(role, user?.id); const invoices = usePortalInvoices(role, user?.id); const orders = usePortalOrders(role, user?.id); const payments = usePortalPayments(role, user?.id); if (!role || !user) return <main className="grid min-h-screen place-items-center bg-[#f7f8fa] p-6"><EmptyState icon={<LogIn className="h-12 w-12 text-[#d1d5db]" />} title="Portal access required" description="Sign in with a customer or vendor account to view only your own documents and payment history." /></main>; const mode = location.pathname.split('/')[2] || 'overview'; const isVendor = role === 'VENDOR'; const labels = { invoices: isVendor ? 'Vendor Bills' : 'Customer Invoices', orders: isVendor ? 'Purchase Orders' : 'Sales Orders' }; return <div className="min-h-screen bg-[#f7f8fa]"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><div><h1 className="font-semibold text-[#1a2332]">Urban Furniture Portal</h1><p className="text-xs text-[#6b7280]">Signed in as {user.name}</p></div><span className="rounded-full bg-[#eef2f6] px-3 py-1 text-xs font-medium">{isVendor ? 'Vendor' : 'Customer'} Portal</span></div></header><div className="mx-auto max-w-6xl px-5 py-6"><nav className="mb-6 flex gap-1 overflow-x-auto border-b">{links.map(link => { const Icon = link.icon; return <NavLink key={link.path} to={link.path} end={link.path === '/portal'} className={({ isActive }) => `flex items-center gap-2 whitespace-nowrap px-3 py-2 text-sm ${isActive ? 'border-b-2 border-[#1a2a3a] text-[#1a2332]' : 'text-[#6b7280]'}`}><Icon className="h-4 w-4" />{link.label}</NavLink>; })}</nav>{mode === 'overview' && <><h2 className="mb-1 text-xl font-semibold">Overview</h2><p className="mb-4 text-sm text-[#6b7280]">Your account activity at a glance.</p>{dashboard.isLoading ? <div className="rounded-lg border bg-white p-8 text-sm text-[#6b7280]">Loading portal…</div> : dashboard.data && <><div className="mb-5 grid gap-3 sm:grid-cols-2"><div className="rounded-lg border bg-white p-4"><p className="text-xs text-[#6b7280]">{isVendor ? 'Open Bills' : 'Open Invoices'}</p><p className="mt-1 text-lg font-semibold">{dashboard.data.openCount}</p></div><div className="rounded-lg border bg-white p-4"><p className="text-xs text-[#6b7280]">Outstanding Amount</p><p className="mt-1 text-lg font-semibold">{formatCurrency(dashboard.data.outstandingAmount)}</p></div></div><h3 className="mb-2 font-medium">Recent {isVendor ? 'Purchase Orders' : 'Orders'}</h3><Table headings={['Number', 'Date', 'Total', 'Status']} >{dashboard.data.recentOrders.map(order => <tr key={order.id} className="border-t"><td className="p-4">{order.number}</td><td className="p-4">{order.date}</td><td className="p-4">{formatCurrency(order.total)}</td><td className="p-4">{order.status}</td></tr>)}</Table></>}</>}{mode === 'invoices' && <><h2 className="mb-4 text-xl font-semibold">{labels.invoices}</h2>{invoices.data?.length ? <Table headings={['Number', 'Date', 'Due Date', 'Total', 'Paid', 'Balance', 'Status']}>{invoices.data.map(invoice => <tr key={invoice.id} className="border-t"><td className="p-4">{invoice.number}</td><td className="p-4">{invoice.date}</td><td className="p-4">{invoice.dueDate || '—'}</td><td className="p-4">{formatCurrency(invoice.total)}</td><td className="p-4">{formatCurrency(invoice.paid)}</td><td className="p-4">{formatCurrency(invoice.balance)}</td><td className="p-4">{invoice.status}</td></tr>)}</Table> : <EmptyState icon={<FileText className="h-12 w-12 text-[#d1d5db]" />} title={`No ${labels.invoices.toLowerCase()}`} description="There are no documents to show." />}</>}{mode === 'orders' && <><h2 className="mb-4 text-xl font-semibold">{labels.orders}</h2>{orders.data?.length ? <Table headings={['Number', 'Date', 'Total', 'Status']}>{orders.data.map(order => <tr key={order.id} className="border-t"><td className="p-4">{order.number}</td><td className="p-4">{order.date}</td><td className="p-4">{formatCurrency(order.total)}</td><td className="p-4">{order.status}</td></tr>)}</Table> : <EmptyState icon={<ShoppingCart className="h-12 w-12 text-[#d1d5db]" />} title="No orders" description="There are no orders to show." />}</>}{mode === 'payments' && <><h2 className="mb-4 text-xl font-semibold">Payment History</h2>{payments.data?.length ? <Table headings={['Payment Number', 'Date', 'Method', 'Reference', 'Amount']}>{payments.data.map(payment => <tr key={payment.id} className="border-t"><td className="p-4">{payment.number}</td><td className="p-4">{payment.date}</td><td className="p-4">{payment.method}</td><td className="p-4">{payment.reference || '—'}</td><td className="p-4">{formatCurrency(payment.amount)}</td></tr>)}</Table> : <EmptyState icon={<ReceiptText className="h-12 w-12 text-[#d1d5db]" />} title="No payments" description="There is no payment history to show." />}</>}</div></div>; }
