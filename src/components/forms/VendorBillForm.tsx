import { useFieldArray, useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Plus, Trash2 } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { formatCurrency } from '@/lib/format';
import { useContacts } from '@/hooks/useContacts';
import { useProducts } from '@/hooks/useProducts';
import { usePurchaseOrders } from '@/hooks/usePurchaseOrders';
import type { VendorBill, VendorBillInput } from '@/types/vendorBill';

const itemSchema = z.object({ productId: z.string().min(1, 'Product is required'), quantity: z.number().positive('Quantity must be greater than 0'), unitPrice: z.number().min(0, 'Unit price cannot be negative'), taxRate: z.number().min(0, 'Tax rate cannot be negative') });
const schema = z.object({ billNumber: z.string().min(1, 'Bill number is required'), billDate: z.string().min(1, 'Bill date is required'), dueDate: z.string().optional(), purchaseOrderId: z.string().optional(), vendorId: z.string().min(1, 'Vendor is required'), items: z.array(itemSchema).min(1, 'Add at least one item'), notes: z.string().optional() }).refine(value => !value.dueDate || value.dueDate >= value.billDate, { message: 'Due date must be on or after bill date', path: ['dueDate'] });
type Values = z.infer<typeof schema>; type Props = { initialData?: VendorBill | null; onSubmit: (input: VendorBillInput) => void; onCancel: () => void; isSubmitting: boolean };
const controlClass = 'w-full rounded-md border border-[#e5e7eb] bg-white px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-[#1a2a3a]';

export function VendorBillForm({ initialData, onSubmit, onCancel, isSubmitting }: Props) {
  const { data: contacts = [] } = useContacts({ type: 'ALL' }); const { data: products = [] } = useProducts({}); const { data: purchaseOrders = [] } = usePurchaseOrders({ status: 'Confirmed' });
  const vendors = contacts.filter(value => value.type === 'VENDOR' || value.type === 'BOTH');
  const { register, handleSubmit, control, watch, setValue, formState: { errors } } = useForm<Values>({ resolver: zodResolver(schema), defaultValues: { billNumber: initialData?.billNumber ?? '', billDate: initialData?.billDate ?? new Date().toISOString().slice(0, 10), dueDate: initialData?.dueDate ?? '', purchaseOrderId: initialData?.purchaseOrderId ?? '', vendorId: initialData?.vendorId ?? '', items: initialData?.items.map(({ productId, quantity, unitPrice, taxRate }) => ({ productId, quantity, unitPrice, taxRate })) ?? [{ productId: '', quantity: 1, unitPrice: 0, taxRate: 0 }], notes: initialData?.notes ?? '' } });
  const { fields, append, remove, replace } = useFieldArray({ control, name: 'items' }); const items = watch('items'); const subtotal = items.reduce((total, item) => total + (item.quantity || 0) * (item.unitPrice || 0), 0); const tax = items.reduce((total, item) => total + (item.quantity || 0) * (item.unitPrice || 0) * (item.taxRate || 0), 0);
  const selectPO = (id: string) => { setValue('purchaseOrderId', id); const po = purchaseOrders.find(value => value.id === id); if (po) { setValue('vendorId', po.vendorId); replace(po.items.map(item => ({ productId: item.productId, quantity: item.quantity, unitPrice: item.unitPrice, taxRate: item.taxRate }))); } };
  return <form className="space-y-5" onSubmit={handleSubmit(data => onSubmit(data))}><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="space-y-1"><label htmlFor="bill-number" className="text-sm font-medium">Bill Number *</label><input id="bill-number" className={controlClass} {...register('billNumber')} />{errors.billNumber && <p className="text-xs text-red-600">{errors.billNumber.message}</p>}</div><div className="space-y-1"><label htmlFor="bill-date" className="text-sm font-medium">Bill Date *</label><input id="bill-date" type="date" className={controlClass} {...register('billDate')} /></div><div className="space-y-1"><label htmlFor="due-date" className="text-sm font-medium">Due Date</label><input id="due-date" type="date" className={controlClass} {...register('dueDate')} />{errors.dueDate && <p className="text-xs text-red-600">{errors.dueDate.message}</p>}</div><div className="space-y-1"><label htmlFor="bill-vendor" className="text-sm font-medium">Vendor *</label><select id="bill-vendor" className={controlClass} {...register('vendorId')}><option value="">Select a vendor</option>{vendors.map(vendor => <option key={vendor.id} value={vendor.id}>{vendor.name}</option>)}</select>{errors.vendorId && <p className="text-xs text-red-600">{errors.vendorId.message}</p>}</div></div><div className="space-y-1"><label htmlFor="bill-po" className="text-sm font-medium">Purchase Order</label><select id="bill-po" className={controlClass} value={watch('purchaseOrderId') || ''} onChange={event => selectPO(event.target.value)}><option value="">No purchase order</option>{purchaseOrders.map(order => <option key={order.id} value={order.id}>{order.orderNumber} — {order.vendorName}</option>)}</select><p className="text-xs text-[#6b7280]">Selecting a confirmed purchase order loads its vendor and line items.</p></div><section><div className="mb-2 flex items-center justify-between"><label className="text-sm font-medium">Bill Items *</label><Button type="button" size="sm" variant="outline" onClick={() => append({ productId: '', quantity: 1, unitPrice: 0, taxRate: 0 })}><Plus className="mr-1 h-4 w-4" />Add Item</Button></div><div className="overflow-x-auto rounded-md border border-[#e5e7eb]"><table className="min-w-[720px] w-full text-sm"><thead className="bg-[#f9fafb] text-left text-xs uppercase text-[#6b7280]"><tr><th className="p-3">Product</th><th className="p-3">Qty</th><th className="p-3">Unit Price</th><th className="p-3">Tax %</th><th className="p-3 text-right">Line Total</th><th className="p-3" /></tr></thead><tbody>{fields.map((field, index) => { const item = items[index]; const total = (item?.quantity || 0) * (item?.unitPrice || 0) * (1 + (item?.taxRate || 0)); return <tr key={field.id} className="border-t"><td className="p-2"><select className={controlClass} {...register(`items.${index}.productId`)} onChange={event => { register(`items.${index}.productId`).onChange(event); const product = products.find(value => value.id === event.target.value); if (product) setValue(`items.${index}.unitPrice`, product.costPrice); }}><option value="">Select product</option>{products.map(product => <option key={product.id} value={product.id}>{product.name}</option>)}</select></td><td className="p-2"><input type="number" step="0.01" className="w-20 rounded border p-2" {...register(`items.${index}.quantity`, { valueAsNumber: true })} /></td><td className="p-2"><input type="number" step="0.01" className="w-28 rounded border p-2" {...register(`items.${index}.unitPrice`, { valueAsNumber: true })} /></td><td className="p-2"><input type="number" step="0.01" className="w-20 rounded border p-2" {...register(`items.${index}.taxRate`, { valueAsNumber: true })} /></td><td className="p-2 text-right font-medium">{formatCurrency(total)}</td><td className="p-2 text-center"><button type="button" disabled={fields.length === 1} aria-label="Remove item" onClick={() => remove(index)} className="text-[#6b7280] hover:text-red-600 disabled:opacity-30"><Trash2 className="h-4 w-4" /></button></td></tr>; })}</tbody><tfoot className="border-t bg-[#f9fafb]"><tr><td colSpan={4} className="p-2 text-right">Subtotal</td><td className="p-2 text-right">{formatCurrency(subtotal)}</td><td /></tr><tr><td colSpan={4} className="p-2 text-right">Tax</td><td className="p-2 text-right">{formatCurrency(tax)}</td><td /></tr><tr><td colSpan={4} className="p-2 text-right font-semibold">Total</td><td className="p-2 text-right font-semibold">{formatCurrency(subtotal + tax)}</td><td /></tr></tfoot></table></div></section><div className="grid gap-3 rounded-md bg-[#f9fafb] p-3 text-sm sm:grid-cols-3"><div><span className="block text-xs text-[#6b7280]">Total Amount</span>{formatCurrency(subtotal + tax)}</div><div><span className="block text-xs text-[#6b7280]">Paid Amount</span>{formatCurrency(initialData?.paidAmount ?? 0)}</div><div><span className="block text-xs text-[#6b7280]">Balance Due</span>{formatCurrency((subtotal + tax) - (initialData?.paidAmount ?? 0))}</div></div><div className="space-y-1"><label htmlFor="bill-notes" className="text-sm font-medium">Notes</label><textarea id="bill-notes" rows={3} className={controlClass} {...register('notes')} /></div><div className="flex justify-end gap-3 border-t pt-4"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button type="submit" disabled={isSubmitting} className="bg-[#1a2a3a] hover:bg-[#2a3f56]">{isSubmitting ? 'Saving…' : initialData ? 'Save Changes' : 'Create Vendor Bill'}</Button></div></form>;
}
